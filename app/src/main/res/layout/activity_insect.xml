<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_blue_primary_light"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:fontFamily="@font/poppins_bold"
        android:gravity="center"
        android:text="🐛 Insect Identification"
        android:textColor="#6BAA6E"
        android:textSize="28sp"
        android:textStyle="bold" />

    <!-- Image Display -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginBottom="16dp"
        android:background="#E8F5E9"
        android:contentDescription="Insect Image"
        android:elevation="8dp"
        android:scaleType="centerInside" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/cameraBtn"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:backgroundTint="#6BAA6E"
            android:elevation="8dp"
            android:fontFamily="@font/poppins_semibold"
            android:text="📷 Camera"
            android:textSize="16sp" />

        <Button
            android:id="@+id/galleryBtn"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:backgroundTint="#6BAA6E"
            android:elevation="8dp"
            android:fontFamily="@font/poppins_semibold"
            android:text="🖼️ Gallery"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Back Button -->
    <Button
        android:id="@+id/backBtn"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="#CCCCCC"
        android:elevation="8dp"
        android:fontFamily="@font/poppins_semibold"
        android:text="← Back to Menu"
        android:textSize="14sp" />

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp"
        android:visibility="gone" />

    <!-- Results Display -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/resultText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="#E8F5E9"
                android:elevation="8dp"
                android:fontFamily="@font/poppins_regular"
                android:padding="20dp"
                android:text="Select an image to analyze"
                android:textColor="#1A2B4A"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/insectAgentText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:background="#FFFFFF"
                android:elevation="6dp"
                android:fontFamily="@font/poppins_regular"
                android:padding="18dp"
                android:text="The pest advisor will explain the model result after analysis."
                android:textColor="#102A2A"
                android:textSize="15sp"
                android:visibility="gone" />

            <Button
                android:id="@+id/insectAgentBtn"
                android:layout_width="128dp"
                android:layout_height="128dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:backgroundTint="#6BAA6E"
                android:elevation="8dp"
                android:fontFamily="@font/poppins_bold"
                android:text="Pest\nAgent"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:visibility="gone" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
